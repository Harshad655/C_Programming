//Write a program which accept distance in kilometer and convert it into meter.
(1 kilometer = 1000 Meter)

#include<stdio.h>
#include<conio.h>

int main()
{
    float Km =0.0;
    printf("Enter Distance in Kilometer: ");
    scanf("%f",&Km);

    printf("The distance of Kilometer in Meter is % Meter",km*1000);

    getch();
    return 0;
}
