#include<stdio.h>
#include<conio.h>

int main()
{
    float length = 0.0;
    printf("Enter Side Length Of Square: ");
    scanf("%f",&length);

    printf("Area of Square is %f",length*length);

    getch();
    return 0;
}
